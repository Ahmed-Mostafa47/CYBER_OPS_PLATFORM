# تغيير الباسوورد من البروفايل - دليل الاستخدام

## 🎯 الميزة الجديدة
تم إضافة صفحة جديدة **"Change Password"** تسمح للمستخدمين بتغيير كلمة المرور الخاصة بهم مباشرة من صفحة البروفايل دون الحاجة لـ "Forgot Password".

## 📝 كيفية العمل

### الخطوة 1: دخول البروفايل
1. بعد تسجيل الدخول، انقر على **PROFILE** في القائمة العلوية
2. في صفحة البروفايل، ستجد زر جديد بعنوان **CHANGE_PASSWORD** في قسم "ACCESS_CONTROLS"

### الخطوة 2: ملء نموذج التغيير
عند النقر على الزر، سيتم نقلك إلى صفحة جديدة حيث يجب عليك:

1. **إدخال الباسوورد الحالي (CURRENT_PASSWORD)**
   - هذا للتحقق من أنك أنت صاحب الحساب فعلاً

2. **إدخال الباسوورد الجديد (NEW_PASSWORD)**
   - يجب أن يكون على الأقل 8 أحرف
   - يجب أن يحتوي على حروف كبيرة وصغيرة
   - يجب أن يحتوي على أرقام
   - يجب أن يحتوي على رموز خاصة

3. **تأكيد الباسوورد الجديد (CONFIRM_NEW_PASSWORD)**
   - يجب أن يطابق الباسوورد الجديد

### الخطوة 3: التحقق والتحديث
- سيتم عرض رسالة "Passwords match" إذا تطابقت كلمات المرور
- إذا كان الباسوورد القديم خاطئ:
  - سيظهر رسالة خطأ: **"Current password is incorrect"**
  - ستظهر نصيحة: "Click here to use Forgot Password instead"
  - يمكنك النقر على الرابط للانتقال إلى صفحة Forgot Password

### الخطوة 4: النجاح
- بعد النقر على **UPDATE_PASSWORD**:
  - سيتم تحديث الباسوورد في قاعدة البيانات
  - سيظهر شاشة نجاح جميلة
  - بعد ثانيتين، سيتم نقلك تلقائياً إلى صفحة البروفايل

## 🛡️ الأمان

- ✅ يتم التحقق من الباسوورد القديم قبل السماح بالتحديث
- ✅ يتم تجزئة (hash) الباسوورد الجديد باستخدام BCRYPT
- ✅ يتم التحقق من قوة الباسوورد من جانب العميل والخادم
- ✅ تم تعيين `updated_at` timestamp عند تحديث الباسوورد

## 📁 الملفات المستخدمة

### Frontend:
- `src/components/auth/ChangePasswordPage.jsx` - صفحة تغيير الباسوورد
- `src/components/pages/ProfilePage.jsx` - تم تحديثه لإضافة الزر الجديد
- `src/App.jsx` - تم تحديثه لإضافة الـ route و handlers

### Backend:
- `src/components/auth/change_password.php` - API endpoint لمعالجة تغيير الباسوورد

## 🔌 API Endpoint

### POST `/src/components/auth/change_password.php`

**Request Body:**
```json
{
  "user_id": 1,
  "old_password": "currentPassword123!",
  "new_password": "newPassword456!"
}
```

**Success Response:**
```json
{
  "success": true,
  "message": "Password updated successfully"
}
```

**Error Responses:**
```json
{
  "success": false,
  "message": "Current password is incorrect"
}
```

## ⚙️ التحقق من الأخطاء

يمكنك مراجعة السجلات (error logs) في:
```
C:\xampp\php\logs\php_error_log
```

السكريبت PHP يقوم بتسجيل:
- معرف المستخدم الذي يحاول تغيير الباسوورد
- طول الباسوورد القديم والجديد
- ما إذا كان الباسوورد القديم صحيحاً أم لا
- نتيجة العملية (نجح أم فشل)

## 🧪 الاختبار

### اختبار سريع:
1. تسجيل الدخول بحساب تجريبي
2. الذهاب إلى البروفايل
3. النقر على **CHANGE_PASSWORD**
4. إدخال الباسوورد الحالي (يجب أن يكون صحيح)
5. إدخال باسوورد جديد (يجب أن يطابق المتطلبات)
6. النقر على **UPDATE_PASSWORD**
7. التحقق من النجاح

### اختبار الأخطاء:
- اختبر بباسوورد قديم خاطئ → يجب أن يظهر رسالة خطأ
- اختبر بباسوورد جديد قصير جداً → يجب أن يظهر رسالة خطأ
- اختبر عدم تطابق الباسوورد → يجب أن يظهر مؤشر عدم التطابق

## 📝 ملاحظات إضافية

- تم إنشاء الصفحة بنفس التصميم والأسلوب الموجود في التطبيق (نمط "Hacker")
- تم استخدام الرموز من مكتبة lucide-react
- الصفحة تدعم عرض/إخفاء كلمات المرور
- يتم عرض مؤشر قوة الباسوورد

## 🚀 المستقبل

يمكن تحسين الميزة بإضافة:
- إرسال بريد إلكتروني للتأكيد عند تغيير الباسوورد
- عرض سجل الأنشطة الأمنية
- طلب التحقق من البريد الإلكتروني قبل تغيير الباسوورد
